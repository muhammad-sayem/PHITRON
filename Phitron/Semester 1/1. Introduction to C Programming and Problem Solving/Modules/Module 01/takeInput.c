#include<stdio.h>
int main()
{
    int num1, num3;
    // float num2;
    // char ch;
    // scanf("%d %f %c", &num1, &num2, &ch);
    // printf("%d %.2f %c", num1, num2, ch);
    scanf("%d%% %d%%", &num1, &num3);
    printf("%d%% %d%%",num1, num3);
    return 0;
}
